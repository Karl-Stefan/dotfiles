# --- DECORATION ----------------------------------------------------------------------------------#
decoration {
    rounding = 10

    drop_shadow = yes
    shadow_range = 40
    shadow_render_power = 4
    shadow_ignore_window = true

    col.shadow = $sa
    col.shadow_inactive = $si

    inactive_opacity = 0.8

    blur {
      enabled = true
      size = 10
      passes = 2
      ignore_opacity = true
    }
}
